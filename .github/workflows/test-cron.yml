name: Test Cron
on:
  schedule:
    - cron: '*/1 * * * *'
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Print time
        run: echo "Current time is $(date)"
